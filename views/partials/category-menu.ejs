<div class="list-group">
  <a href="/blogs" class="list-group-item list-group-item-action <%= selectedid == null ? "active" : "" %>"
    >Tüm Bloglar</a
  >
  <hr />
  <% categories.forEach(category => { %>

  <a
    href="/blogs/category/<%= category._id %>"
    class="list-group-item list-group-item-action <%= selectedid == category._id ? "active" : "" %>"
    ><%= category.name %></a
  >
  <% }) %>
</div>
